// Ant Design theme configuration
export const antdTheme = {
  token: {
    // Customize theme tokens here if needed
    colorPrimary: '#1890ff',
  },
  components: {
    // Customize component styles here if needed
  },
};

// Note: Warning suppression is now handled in suppress-warnings.js
// This function is kept for backward compatibility but does nothing
export const suppressAntdWarnings = () => {
  // Warning suppression is handled in suppress-warnings.js
  // This function is kept for backward compatibility
};
